import React, { useEffect, useState } from 'react'
// import { getFileWithName } from '../gdriveService';
import useAsync from '../hooks/useAsync'

type CredsListProps = {}

export const CredsList = ({}: CredsListProps) => {
	// getFileWithName('test');
	// const findFileWithName = () => getFileWithName('test');

	// const { value, status, error } = useAsync(findFileWithName);
	// useEffect(() => {}, []);

	return (
		<>
			{/* {status === 'pending' && <h1>Loading...</h1>}
			{status === 'error' && console.error(error)} */}
		</>
	)
}
