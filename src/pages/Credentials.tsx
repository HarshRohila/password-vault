import React, { useEffect, useState } from 'react';

type CredentialsPageProps = {};

export const CredentialsPage = ({}: CredentialsPageProps) => {
	const [state, setState] = useState();

	useEffect(() => {}, []);

	return (
		<>
			<h1>React TS FC Component</h1>
			<div>List</div>
		</>
	);
};
